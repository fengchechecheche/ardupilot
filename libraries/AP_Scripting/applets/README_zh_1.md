# Lua 小程序

与示例目录中的代码不同，这些都是完整的小程序，用户无需编辑文件即可使用。每个小程序都配有一个 .md 文件，描述其操作方法以及在 ArduPilot 中运行所需的任何开关或参数。用户只需将小程序复制到 SD 卡的 APM/scripts 目录中即可完成安装。当然，必须通过设置 SCR_ENABLE = 1 来启用脚本功能，并且自动驾驶仪需支持运行脚本。有关 Lua 脚本的详细信息，请参阅高级配置下的 Wiki 页面。

每个小程序都将在 Wiki 中有一个对应的条目，其内容与 .md 文件一致，并会提供返回此目录的链接。