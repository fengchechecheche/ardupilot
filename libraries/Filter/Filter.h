#pragma once

/* Umbrella header for the Filter library */

// 导入一个名为“FilterClass”的头文件。
// 这可能包含了一个基础过滤器的类定义，或者是与过滤器相关的某些通用功能。
#include "FilterClass.h"

// 导入一个名为“AverageFilter”的头文件。
// 这可能定义了一个平均滤波器，用于计算数据的平均值。
#include "AverageFilter.h"

// 导入一个名为“DerivativeFilter”的头文件。
// 这可能定义了一个求导滤波器，用于计算数据的导数或变化率。
#include "DerivativeFilter.h"

// 导入一个名为“FilterWithBuffer”的头文件。
// 这可能定义了一个带有缓冲区的过滤器，用于存储或处理数据流。
#include "FilterWithBuffer.h"

// 导入一个名为“LowPassFilter”的头文件。
// 这定义了一个低通滤波器，用于允许低频信号通过并阻止高频信号。
#include "LowPassFilter.h"

// 导入一个名为“ModeFilter”的头文件。
// 这可能定义了一个模式滤波器，用于确定或识别数据中的常见模式或值。
#include "ModeFilter.h"

// 导入一个名为“Butter”的头文件。
// 从名称来看，这可能定义了一个Butterworth滤波器，它是一种特定的滤波器类型，具有平滑的频率响应特性。
#include "Butter.h"
